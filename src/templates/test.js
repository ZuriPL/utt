import { Test } from "utt"

/* 
 * Hover over a method defintion to see what it does
 * and what helper methods you might want to use
*/

export default class extends Test {
    args() {
        return [ "" ];
    }

    input() {
        
    }

    transform() {
        return [ ]  
    }

    files() {

    }

    check() {
        return this.defaultChecks
    }
}
